library(cluster)
library(rattle)
library(factoextra)
library(NbClust)

df<-read.csv(data)
str(data)

#adf is final saved manipulated data
adf<-read.csv(data)
str(adf)

fviz_nbcluster(adf,kmeans,method="wss") + geom_vline(xintercept=2,linetype=2)+labs(subtitle="Elbow Method")
